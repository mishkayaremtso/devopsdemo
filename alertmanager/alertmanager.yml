route:
  receiver: 'alertmananger-bot'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h
  group_by: ['instance']

receivers:

- name: alertmananger-bot
  webhook_configs:
  - send_resolved: true
    url: 'http://prometheus-alertmanager_bot:8080'
